colorscheme hybrid

if has('unix')
  if has('mac')
    set guifont=Fira_Code:h13
    set macligatures
    set shell=/bin/bash
    set guioptions-=T
    set guioptions-=r
    set guioptions-=L
  else
  endif
endif
